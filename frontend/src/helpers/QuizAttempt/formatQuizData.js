const formatQuizData = (data) => {
    // Aquí puedes formatear los datos según tus necesidades
    // Por ejemplo, puedes convertir las fechas de inicio y fin a un formato más legible
    return data;
  };
  
  export default formatQuizData;
  